# Water Meter AI - CPU Only Dependencies (for Docker)
# Much smaller than CUDA version

# FastAPI Server
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.0
python-multipart==0.0.12

# Deep Learning - CPU ONLY (much smaller!)
# Install torch from PyTorch CPU repository
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.0+cpu
torchvision==0.20.0+cpu
ultralytics==8.3.0

# Image Processing (headless for Docker/server)
opencv-python-headless==4.10.0.84
pillow==10.4.0
numpy==1.26.4

# Utilities
pandas==2.2.3
tqdm==4.66.5

# HTTP client for health checks
requests==2.32.3
